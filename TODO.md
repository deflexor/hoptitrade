Add SQLite, save all positions there, filter opportunities to make sure that there is no already open position for that options/strikes to prevent duplicate positions.
After restart check in database i we have any currently open positions to keep tracking them/

